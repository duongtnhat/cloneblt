<div class="container body-content">
  <section class="manga-detail">
    <article class="list-chapter ajax-content" data-url="/ajax/Chapter/LoadListChapter?id=5618">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">Danh sách chap (<%= @items.count %>)</h3>
        </div>
        <div class="panel-body">
          <ul class="list-unstyled" id="listChapter">
            <% @items.each do |item| %>
              <li>
                <div class="row">
                  <div class="col-sm-7 col-xs-6">
                    <a href="<%= item[:link] %>"><%= item[:title] %></a>
                  </div>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </article>
  </section>
</div>